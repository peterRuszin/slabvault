services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: slabvault
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - slabvault_data:/var/lib/postgresql/data

volumes:
  slabvault_data: